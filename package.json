{
  "name": "sfera-zone",
  "version": "0.0.1",
  "description": "Верстка сайта sfera.zone.",
  "main": "index.js",
  "scripts": {
    "start": "webpack serve --mode=development",
    "build": "webpack --mode=production --progress",
    "preview": "npm run build && http-server dist -og",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "clean-webpack-plugin": "^4.0.0",
    "css-loader": "^6.7.1",
    "gh-pages": "^4.0.0",
    "http-server": "^14.1.1",
    "pug-plugin": "^4.5.0",
    "sass": "^1.54.1",
    "sass-loader": "^13.0.2",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0",
    "webpack-dev-server": "^4.9.3"
  }
}